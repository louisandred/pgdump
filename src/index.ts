import { DumpOptions, dumpSchema } from "@/dump";

/**
 * Public API for pgdump
 */
export { DumpOptions, dumpSchema };

/**
 * For convenience, we also export a default object.
 */
export default {
	dumpSchema,
};
